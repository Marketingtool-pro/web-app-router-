echo "🔍 Running pre-commit checks..."

# Build check
echo "🏗️ Checking build..."
npm run build 2>&1 | tail -5 || { echo "❌ Build errors found. Fix before committing."; exit 1; }

# Lint-staged (only staged files)
npx lint-staged

echo "✅ Pre-commit checks passed!"
